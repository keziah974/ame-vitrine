{% extends 'base.html.twig' %}

{% block body %}
    <section class="min-h-screen bg-black flex items-center justify-center px-6 md:px-8 py-16 text-white">
        <div class="relative max-w-4xl w-full">
            <div class="absolute inset-0 rounded-xl bg-gradient-to-r from-[#ff5ca5] to-[#a855f7] blur-xl opacity-50"></div>
            <div class="relative bg-gray-950 rounded-xl p-6 shadow-lg">
                <h2 class="text-3xl font-bold text-center mb-6">Notre carte des cocktails</h2>
                <img 
                    src="{{ asset('images/carte-cocktails.jpeg') }}" 
                    alt="Carte des cocktails" 
                    class="w-full h-auto object-contain rounded-lg"
                />
                <div class="text-center mt-6">
                    <a href="{{ path('home') }}#about" class="btn bg-gradient-to-r from-purple-600 via-purple-500 to-purple-400 hover:from-purple-700 hover:via-purple-600 hover:to-purple-500 text-white font-semibold transition-colors duration-300">
                        Retour à la page d'accueil
                    </a>
                </div>
            </div>
        </div>
    </section>
{% endblock %}